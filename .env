NODE_ENV="development"
PORT=3000

# SQLite (default — no server needed, great for local dev)
DATABASE_URL="file:./prisma/dev.db"

# PostgreSQL — update prisma/schema.prisma provider to "postgresql" and
# update prisma.config.ts datasource.url to use process.env['DATABASE_URL']
# DATABASE_URL="postgresql://user:password@localhost:5432/court_reporting"